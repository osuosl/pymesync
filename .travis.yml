language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"

notifications:
  email: false

# command to install dependencies
install:
  - pip install -r requirements.txt

script:
  - make verify
